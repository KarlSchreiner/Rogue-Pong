export enum sides {
  player = "player",
  ai = "ai",
  common = "common",
}

export enum sliderColors {
  player = "blue",
  ai = "red",
  common = "green",
}
export enum paddleTypes {
  regular = "regular",
  zombie = "zombie",
}
